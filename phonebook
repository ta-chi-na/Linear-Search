#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string names[] = {"Carter", "Jessica"};
    string numbers[] = {"+1-876-098-6543", "+1-905-964-1925"};

    for (int i = 0; i < 2; i++)
    {
        if (strcmp(names[i], "Jessica") == 0)
        {
            printf("Fuond: %s\n", numbers[i]);
            return 0;
        }
    }
    printf("Not Found\n");
    return 1;
}
